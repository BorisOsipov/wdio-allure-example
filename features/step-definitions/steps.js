import {defineSupportCode} from 'cucumber'

export default defineSupportCode(function ({Given, When, Then}) {
    When('I create a user:', (url) => {

    });
    Then('the Edit User page validation elements are confirmed', (url) => {

    });


    Then('I confirm the user has been created successfully', () => {

    });

})
